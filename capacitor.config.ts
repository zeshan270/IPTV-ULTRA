import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iptv.ultra',
  appName: 'IPTV ULTRA',
  webDir: 'out',
  server: { androidScheme: 'https' },
};

export default config;
